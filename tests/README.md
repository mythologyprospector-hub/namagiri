# Tests

Tests are staged according to `docs/TESTING.md`.

`fixtures/` contains controlled ELF test inputs.

`integration/` contains tests that require an actual Shiva build.

Do not place arbitrary personal binaries or unrelated system files here.
