# Configuration

Configuration belongs here only when it describes Namagiri behavior.

Do not encode assumptions about Shiva that are not supported by the authoritative source.

Planned early configuration areas:

- Shiva executable path;
- Shiva runtime module path;
- result directory;
- safety/approval policy.
